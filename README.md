# Gloutonnerie

Turn scanned comic book pages into a vertical, scrollable webtoon format.

Classic comics are laid out as full pages meant to be read on paper. This tool detects individual panels automatically, orders them by reading sequence, and re-stacks them vertically for comfortable reading on a phone.

Everything runs client-side in the browser — no upload, no server, no data leaves your machine.
